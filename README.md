# Deprecated: Circles credentials for Go

The canonical Go module now lives in the public multi-language repository:

```go
import credentials "github.com/circlesac/credentials/go"
```

Use `github.com/circlesac/credentials/go@v1.2.2` or later. This repository is
retained only so existing module versions remain downloadable.
